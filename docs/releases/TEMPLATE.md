<!-- Copy to v<version>.md and fill in. Delete empty sections and these comments.
     The release workflow publishes this file VERBATIM as the release page, and
     refuses to release without it. GitHub renders full Markdown here: headings,
     bold, emoji, images and <details> all work — but EVERY newline is a hard
     line break, so keep each paragraph and each bullet on ONE source line or
     the page breaks mid-sentence. -->

## ✨ Highlights

<!-- One to three sentences a player skims: the reason this release exists.
     If anything visible changed, a screenshot here is worth all the bullets:
     ![caption](https://github.com/SalehBusbait/RimManager/raw/main/assets/screenshots/....png) -->

## 🚀 New

- **Bold lead** — one line of what it does for the player.

## 🛠️ Fixed

- **What was wrong** — stated plainly, including what it used to do.

## 🔄 Changed

## 🗑️ Removed

## 📦 Downloads

<!-- The assets list renders below the notes automatically; this row just routes. -->
**Windows:** run `RimManager-Setup-<version>.exe` (or take the portable `win-x64` zip)
· **macOS:** `osx-arm64` for Apple Silicon, `osx-x64` for Intel · **Linux:** `linux-x64`

**Full changelog**: [v<prev>...v<version>](https://github.com/SalehBusbait/RimManager/compare/v<prev>...v<version>)
