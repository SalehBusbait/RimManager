<!-- Thank you for contributing. A short, scoped pull request is the easiest
     kind to review and merge. -->

## What this changes

<!-- One or two sentences: what it does, and why. -->

## How it was verified

<!-- dotnet test RimManager.slnx -c Release must pass. For UI changes, a
     before/after screenshot is worth more than the diff — Avalonia fails
     silently, and a picture catches what a test cannot. -->

- [ ] `dotnet test RimManager.slnx -c Release` passes (warnings are errors)
- [ ] UI change — screenshots attached
- [ ] Behavioural change — covered by a test
